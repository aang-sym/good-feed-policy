# Good Feed — Policy Page

Hosts the privacy policy and support page for the Good Feed iOS app, published via
GitHub Pages at whatever URL this repo's Pages settings report.

This repo intentionally contains nothing but that one page — no app source, no
internal docs. It exists so the main app repo can stay private without needing a
paid GitHub plan just to keep this page live.
